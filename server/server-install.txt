These should reside on your server.  It talks to your 'gateway' over a socket connection.

You'll also need to install the sample mysql database on this machine as well.

mysql -u [user] -p [database_name] < trackerdb.sql

